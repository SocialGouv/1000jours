import type { ReactElement } from "react";
import React from "react";
import { View } from "react-native";

export const TabParenthequeVideos = (): ReactElement => {
  return <View />;
};

export default TabParenthequeVideos;
